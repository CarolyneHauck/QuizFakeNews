import 'package:flutter/material.dart';

class Loading {
  start() {
    return Center(child: CircularProgressIndicator());
  }

  loading() {
    return Center(child: CircularProgressIndicator());
  }
}
